# 🐍 Simple RAG demo

A simple RAG demo

---

## 📦 Installation

### Using `venv` (recommended)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

### Create .env, add google gemini api key
GEMINI_API_KEY=xxx

---

## Running
```bash
python3 main.py
